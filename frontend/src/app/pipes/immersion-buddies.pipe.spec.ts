import { ImmersionBuddiesPipe } from './immersion-buddies.pipe';

describe('ImmersionBuddiesPipe', () => {
  it('create an instance', () => {
    const pipe = new ImmersionBuddiesPipe();
    expect(pipe).toBeTruthy();
  });
});
